#include <iostream>

using namespace std;

int main() {
    cout << "Hello World" << endl;
    cout << "This project contains the code samples from my Front Range Community College Intro to Programming C++ Course that I took concurrently in 12th grade in Fall 2021" << endl;
    cout << "Select a file to run the sample. Some samples may return 'Could not open file,' as those samples were assigned with a hypothetical file in mind, and thus they cannot actually be run, but rather written with the capability of running said file if it were present." << endl;
    return 0;
}
